# XcodeGen / Xcode (generowane — odtwarzane przez `xcodegen generate`)
*.xcodeproj
/Generated/

# Build
.build/
DerivedData/
*.xcresult

# Signing material — the self-signed cert export must never be committed
*.p12
*.cer
*.pem

# macOS
.DS_Store

# Claude Code
.claude/scheduled_tasks.lock
.impeccable/

# graphify (generated knowledge graph)
graphify-out/
